import 'package:flutter_filesundefined/index.dart';

class ReactReactRepository {
  final ReactReactProvider _reactReactProvider = ReactReactProvider();

  ReactReactRepository();

  void test(bool isError) {
    _reactReactProvider.test(isError);
  }
}